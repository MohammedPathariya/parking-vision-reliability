# Configuration

Store versioned, non-secret configuration here, including:

- Camera metadata
- Parking-space polygons
- Frame-sampling settings
- Diagnostic thresholds
- Experiment definitions

Do not store credentials, private stream URLs, or customer configuration.
